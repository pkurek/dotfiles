# How To

```
$ mkdir -p ~/.vim/bundle
$ git clone https://github.com/Shougo/neobundle.vim ~/.vim/bundle/neobundle.vim
$ git clone git@github.com:pkurek/dotfiles.git ~/.vim
$ ln -s ~/dotfiles/vimrc ~/.vimrc
```
