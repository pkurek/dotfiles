# How To

```
$ git clone https://github.com/pkurek/dotfiles.git ~/.vim
$ mkdir ~/.vim/bundle
$ git clone https://github.com/Shougo/neobundle.vim ~/.vim/bundle/neobundle.vim
$ ln -s ~/.vim/vimrc ~/.vimrc
```
